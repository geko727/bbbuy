<%= form_for(:series) do |f| %>
  <% if @series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@series.errors.count, "error") %> prohibited this series from being saved:</h2>

      <ul>
      <% @series.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :currency %><br>
    <%= f.select :currency, [['MX','MX'],["USD","USD"]] %>
  </div>
  <div class="field">
    <%= label :coupons, "coupons" %><br>
    <%= text_area :allcoupons, "allcoupons" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
